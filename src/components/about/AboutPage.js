import React from 'react';

const AboutPage = () => (
    <div>
        <h2>About</h2>
        <p>This tutorial starts with the  <a href="https://reactjs.org/tutorial/tutorial.html">React.com</a> tutorial then extends it with routing, Redux, Bootstrap and other common libraries</p>
    </div>
);

export default AboutPage;
